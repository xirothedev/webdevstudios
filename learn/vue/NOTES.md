# NOTES

- Lesson language: **tiếng Việt** (user chọn 2026-09-01).
- User vững React (hooks, react-hook-form, Next App Router) — đã review qua bản port apps/web-vue. Neo mọi khái niệm Vue vào React tương đương.
- Hay hỏi bằng một dòng code (`v-if="!isLast"`, `v-for`, `<script setup>`) → lesson nên luôn có mục "đọc một dòng code thật".
- Workspace nằm trong repo: `learn/vue/` (untracked — đừng commit trừ khi user yêu cầu).
- Code ví dụ ưu tiên lấy từ apps/web-vue (login.vue, checkout.vue) để user thấy tính liên tục với công việc.
