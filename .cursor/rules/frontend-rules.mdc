---
globs: apps/web/**/*.{ts,tsx,js,jsx}
alwaysApply: false
---

# Frontend Rules - Next.js 16 & React

## 🎯 Core Principles

### React Server Components First
- **Priority**: Ưu tiên Server Components, chỉ dùng `'use client'` khi thực sự cần thiết
- **When to use 'use client'**:
  - Interactive components (onClick, onChange, useState, useEffect)
  - Browser APIs (localStorage, window, document)
  - Third-party libraries yêu cầu client-side
  - Animations với motion/react (cần browser APIs)

### Component Structure
```typescript
// ✅ Good: Server Component (default)
export default function ProductPage() {
  // Fetch data, no client-side logic
}

// ✅ Good: Client Component (when needed)
'use client'
export function ProductActions() {
  const [quantity, setQuantity] = useState(1)
  // Interactive logic
}
```

## 📁 File & Directory Conventions

### Naming
- **Files**: `kebab-case` (e.g., `product-actions.tsx`)
- **Components**: `PascalCase` (e.g., `ProductActions`)
- **Directories**: `kebab-case` (e.g., `components/shop/`)
- **Types**: `PascalCase` với suffix `Type` hoặc `Props` (e.g., `ProductType`, `ProductActionsProps`)

### Directory Structure
```
src/
├── app/              # Next.js App Router pages
│   ├── layout.tsx    # Root layout
│   ├── page.tsx      # Home page
│   └── shop/         # Shop routes
├── components/       # Reusable components
│   ├── ui/          # shadcn/ui components
│   ├── wds/         # WebDev Studios components
│   └── shop/        # E-commerce components
├── lib/             # Utilities & helpers
├── types/           # TypeScript types
└── data/            # Static data (temporary, sẽ migrate sang API)
```

## 🎨 Styling Rules

### Tailwind CSS v4
- **Approach**: Utility-first, mobile-first
- **Custom Utilities**: Sử dụng `@layer utilities` trong `globals.css`
- **Theme Colors**: Sử dụng CSS variables cho WDS colors
  ```css
  --wds-black: #000000;
  --wds-white: #ffffff;
  --wds-orange: #f7931e;
  --wds-cream: #fff8e1;
  ```

### Responsive Design
- **Breakpoints**: `sm:`, `md:`, `lg:`, `xl:`, `2xl:`
- **Mobile First**: Viết styles cho mobile trước, sau đó override cho desktop
- **Example**:
  ```tsx
  <div className="text-sm md:text-base lg:text-lg">
    Responsive text
  </div>
  ```

### Image Optimization
- **Format**: WebP (ưu tiên), fallback PNG/JPG
- **Component**: Luôn dùng Next.js `Image` component
- **Required Props**: `src`, `alt`, `width`, `height` hoặc `fill`
- **Priority**: Set `priority={true}` cho images above the fold
- **Sizes**: Luôn specify `sizes` prop cho responsive images
- **Example**:
  ```tsx
  <Image
    src="/image/hero.webp"
    alt="Hero image"
    width={1200}
    height={600}
    priority
    sizes="(max-width: 768px) 100vw, 1200px"
  />
  ```

## 🎭 Animation Rules

### motion/react Library
- **Import**: `import { motion } from 'motion/react'`
- **Performance**: Sử dụng `whileInView` cho scroll animations thay vì `useEffect`
- **Variants**: Định nghĩa animation variants để tái sử dụng
- **Example**:
  ```tsx
  const fadeInUp = {
    hidden: { opacity: 0, y: 20 },
    visible: { opacity: 1, y: 0 }
  }
  
  <motion.div
    initial="hidden"
    whileInView="visible"
    variants={fadeInUp}
    transition={{ duration: 0.5 }}
  >
    Content
  </motion.div>
  ```

## 🔄 State Management

### Server State
- **Data Fetching**: Sử dụng async Server Components
- **Caching**: Next.js built-in caching (fetch cache, route cache)
- **Example**:
  ```tsx
  export default async function ProductPage({ params }) {
    const product = await fetchProduct(params.slug)
    return <ProductDetails product={product} />
  }
  ```

### Client State
- **Simple State**: `useState` cho local component state
- **Form State**: **BẮT BUỘC** sử dụng React Hook Form 7.x.x cho tất cả forms
- **Global State**: Cân nhắc Zustand hoặc Context API (nếu thực sự cần)

### API State Management (TanStack Query) - BẮT BUỘC
- **Library**: `@tanstack/react-query` - **BẮT BUỘC** sử dụng cho tất cả API calls
- **Purpose**: Quản lý server state, caching, synchronization
- **Setup**: Wrap app với `QueryClientProvider`
- **Requirement**: 
  - **KHÔNG BAO GIỜ** gọi API trực tiếp từ component
  - **PHẢI** sử dụng TanStack Query hooks (`useQuery`, `useMutation`)
  - **PHẢI** handler `onSuccess` và `onError` trong TanStack Query mutations
  - **PHẢI** sử dụng Axios (không dùng fetch trực tiếp)
- **Example**:
  ```tsx
  // Setup QueryClient
  const queryClient = new QueryClient({
    defaultOptions: {
      queries: {
        staleTime: 60 * 1000, // 1 minute
        refetchOnWindowFocus: false,
      },
    },
  })
  
  // In app
  <QueryClientProvider client={queryClient}>
    <App />
  </QueryClientProvider>
  ```

## 🛡️ Type Safety

### TypeScript Rules
- **Strict Mode**: Luôn bật TypeScript strict mode
- **Avoid `any`**: Không dùng `any`, dùng `unknown` nếu cần
- **Type Definitions**: Định nghĩa types trong `src/types/`
- **Props Types**: Luôn type props của components
- **Example**:
  ```tsx
  interface ProductActionsProps {
    productId: string
    stock: number
    onAddToCart: (quantity: number) => void
  }
  
  export function ProductActions({ productId, stock, onAddToCart }: ProductActionsProps) {
    // ...
  }
  ```

## 📦 Required Dependencies

### BẮT BUỘC phải có trong package.json:
```json
{
  "dependencies": {
    "@tanstack/react-query": "^5.x.x",
    "react-hook-form": "^7.x.x",
    "@hookform/resolvers": "^3.x.x",
    "zod": "^3.x.x",
    "axios": "^1.x.x"
  }
}
```

**Lưu ý**: 
- TanStack Query: Bắt buộc cho tất cả API calls
- React Hook Form: Bắt buộc cho tất cả forms
- Zod: Bắt buộc cho form validation (dùng với @hookform/resolvers)
- Axios: Bắt buộc cho HTTP client (không dùng fetch)

## 📡 API Integration

### API Client Setup - BẮT BUỘC
- **HTTP Client**: **BẮT BUỘC** sử dụng Axios cho HTTP requests
- **State Management**: **BẮT BUỘC** sử dụng TanStack Query (React Query) cho data fetching và caching
- **Location**: 
  - API client: `src/lib/api-client.ts` (axios instance với withCredentials)
  - API services: `src/lib/api/` (API functions)
  - Query hooks: `src/lib/api/hooks/` (TanStack Query hooks)

### Axios Client Configuration - BẮT BUỘC
**QUAN TRỌNG**: Axios client PHẢI có `withCredentials: true` để gửi cookies (httpOnly cookies).

```tsx
// src/lib/api-client.ts
import axios, { AxiosError, AxiosInstance, InternalAxiosRequestConfig } from 'axios'

const API_URL = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:4000'

// BẮT BUỘC: Tạo axios instance với withCredentials
export const apiClient: AxiosInstance = axios.create({
  baseURL: API_URL,
  headers: {
    'Content-Type': 'application/json',
  },
  withCredentials: true, // BẮT BUỘC: Để gửi httpOnly cookies
})

// Request interceptor - Cookies tự động gửi với withCredentials
apiClient.interceptors.request.use(
  (config: InternalAxiosRequestConfig) => {
    // Cookies tự động gửi bởi browser
    return config
  },
  (error) => {
    return Promise.reject(error)
  }
)

// Response interceptor - Handle errors
apiClient.interceptors.response.use(
  (response) => response,
  (error: AxiosError<{ message?: string; error?: string }>) => {
    // Handle network errors
    if (!error.response) {
      return Promise.reject({
        message: 'Không thể kết nối đến server. Vui lòng thử lại sau.',
        status: 0,
      })
    }

    const status = error.response.status
    const data = error.response.data

    // Extract error message
    let message = data?.message || data?.error || 'Đã xảy ra lỗi'

    // Map status codes to Vietnamese messages
    switch (status) {
      case 400:
        message = data?.message || 'Dữ liệu không hợp lệ'
        break
      case 401:
        message = data?.message || 'Xác thực thất bại. Vui lòng đăng nhập lại.'
        break
      case 403:
        message = 'Bạn không có quyền thực hiện hành động này'
        break
      case 404:
        message = data?.message || 'Không tìm thấy tài nguyên'
        break
      case 409:
        message = data?.message || 'Dữ liệu đã tồn tại'
        break
      case 422:
        message = data?.message || 'Dữ liệu không hợp lệ'
        break
      case 500:
        message = 'Lỗi server. Vui lòng thử lại sau.'
        break
      default:
        message = data?.message || `Lỗi ${status}: ${error.message}`
    }

    return Promise.reject({
      message,
      status,
      data: error.response.data,
    })
  }
)
```

### API Services with Axios
```tsx
// src/lib/api/services/cart.service.ts
import apiClient from '../client'

export interface AddToCartRequest {
  productId: string
  quantity: number
  size?: string
}

export interface CartItem {
  id: string
  productId: string
  quantity: number
  size?: string
}

export const cartService = {
  addToCart: async (data: AddToCartRequest): Promise<CartItem> => {
    const response = await apiClient.post<CartItem>('/cart/add', data)
    return response.data
  },
  
  getCart: async (): Promise<CartItem[]> => {
    const response = await apiClient.get<CartItem[]>('/cart')
    return response.data
  },
  
  updateCartItem: async (itemId: string, quantity: number): Promise<CartItem> => {
    const response = await apiClient.put<CartItem>(`/cart/items/${itemId}`, { quantity })
    return response.data
  },
  
  removeCartItem: async (itemId: string): Promise<void> => {
    await apiClient.delete(`/cart/items/${itemId}`)
  },
}
```

### TanStack Query Hooks - BẮT BUỘC
**QUAN TRỌNG**: Tất cả API calls PHẢI sử dụng TanStack Query hooks. KHÔNG BAO GIỜ gọi API trực tiếp.

```tsx
// src/lib/api/hooks/use-cart.ts
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { toast } from 'sonner'
import { cartService, AddToCartRequest } from '../services/cart.service'

// Query: Get cart items
export function useCart() {
  return useQuery({
    queryKey: ['cart'],
    queryFn: () => cartService.getCart(),
    staleTime: 30 * 1000, // 30 seconds
  })
}

// Mutation: Add to cart - BẮT BUỘC có onSuccess và onError
export function useAddToCart() {
  const queryClient = useQueryClient()
  
  return useMutation({
    mutationFn: (data: AddToCartRequest) => cartService.addToCart(data),
    onSuccess: () => {
      // BẮT BUỘC: Invalidate queries để sync data
      queryClient.invalidateQueries({ queryKey: ['cart'] })
      // BẮT BUỘC: Show success notification
      toast.success('Đã thêm vào giỏ hàng')
    },
    onError: (error: unknown) => {
      // BẮT BUỘC: Handle error và show notification
      const errorMessage = error instanceof Error 
        ? error.message 
        : 'Không thể thêm vào giỏ hàng'
      toast.error(errorMessage)
      console.error('Failed to add to cart:', error)
    },
  })
}

// Mutation: Update cart item - BẮT BUỘC có onSuccess và onError
export function useUpdateCartItem() {
  const queryClient = useQueryClient()
  
  return useMutation({
    mutationFn: ({ itemId, quantity }: { itemId: string; quantity: number }) =>
      cartService.updateCartItem(itemId, quantity),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['cart'] })
      toast.success('Đã cập nhật giỏ hàng')
    },
    onError: (error: unknown) => {
      const errorMessage = error instanceof Error 
        ? error.message 
        : 'Không thể cập nhật giỏ hàng'
      toast.error(errorMessage)
    },
  })
}

// Mutation: Remove cart item - BẮT BUỘC có onSuccess và onError
export function useRemoveCartItem() {
  const queryClient = useQueryClient()
  
  return useMutation({
    mutationFn: (itemId: string) => cartService.removeCartItem(itemId),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['cart'] })
      toast.success('Đã xóa khỏi giỏ hàng')
    },
    onError: (error: unknown) => {
      const errorMessage = error instanceof Error 
        ? error.message 
        : 'Không thể xóa khỏi giỏ hàng'
      toast.error(errorMessage)
    },
  })
}
```

**Rules cho TanStack Query**:
- ✅ **BẮT BUỘC**: Tất cả mutations PHẢI có `onSuccess` và `onError` handlers
- ✅ **BẮT BUỘC**: `onSuccess` PHẢI invalidate related queries
- ✅ **BẮT BUỘC**: `onError` PHẢI show error notification (toast)
- ✅ **BẮT BUỘC**: `onSuccess` có thể show success notification nếu cần
- ✅ **BẮT BUỘC**: Sử dụng Axios (không dùng fetch)
- ❌ **KHÔNG BAO GIỜ**: Gọi API service trực tiếp từ component
- ❌ **KHÔNG BAO GIỜ**: Sử dụng `useEffect` để fetch data (dùng `useQuery` thay thế)

### Using Hooks in Components
**Lưu ý**: `onSuccess` và `onError` đã được handle trong hook definition, không cần handle lại trong component (trừ khi cần custom logic).

```tsx
'use client'
import { useCart, useAddToCart } from '@/lib/api/hooks/use-cart'

export function CartButton({ productId, size }: { productId: string; size?: string }) {
  const { data: cart, isLoading } = useCart()
  const addToCart = useAddToCart()
  
  const handleAddToCart = () => {
    // onSuccess và onError đã được handle trong useAddToCart hook
    // Chỉ cần gọi mutate với data
    addToCart.mutate({ productId, quantity: 1, size })
  }
  
  return (
    <button
      onClick={handleAddToCart}
      disabled={addToCart.isPending || isLoading}
    >
      {addToCart.isPending ? 'Đang thêm...' : 'Thêm vào giỏ'}
    </button>
  )
}
```

**Nếu cần custom logic trong component**:
```tsx
const handleAddToCart = () => {
  addToCart.mutate(
    { productId, quantity: 1, size },
    {
      onSuccess: () => {
        // Custom logic (nếu cần)
        // Note: onSuccess trong hook vẫn chạy trước
        router.push('/cart')
      },
      onError: (error) => {
        // Custom error handling (nếu cần)
        // Note: onError trong hook vẫn chạy trước
        console.log('Custom error handling:', error)
      },
    }
  )
}
```

### Form Handling với React Hook Form 7.x.x - BẮT BUỘC
**QUAN TRỌNG**: Tất cả forms PHẢI sử dụng React Hook Form 7.x.x. KHÔNG BAO GIỜ dùng controlled inputs với useState.

```tsx
'use client'
import { useForm } from 'react-hook-form'
import { zodResolver } from '@hookform/resolvers/zod'
import { z } from 'zod'
import { useMutation } from '@tanstack/react-query'
import { toast } from 'sonner'
import { authApi } from '@/lib/api/auth.api'

// Define validation schema với Zod
const loginSchema = z.object({
  email: z.string().email('Email không hợp lệ'),
  password: z.string().min(8, 'Mật khẩu phải có ít nhất 8 ký tự'),
  rememberMe: z.boolean().optional(),
})

type LoginFormData = z.infer<typeof loginSchema>

export function LoginForm() {
  const {
    register,
    handleSubmit,
    formState: { errors, isSubmitting },
  } = useForm<LoginFormData>({
    resolver: zodResolver(loginSchema),
    defaultValues: {
      rememberMe: false,
    },
  })

  // BẮT BUỘC: Sử dụng TanStack Query mutation
  const loginMutation = useMutation({
    mutationFn: (data: LoginFormData) => authApi.login(data),
    onSuccess: () => {
      toast.success('Đăng nhập thành công!')
      router.push('/')
    },
    onError: (error: unknown) => {
      const errorMessage = error instanceof Error 
        ? error.message 
        : 'Đăng nhập thất bại'
      toast.error(errorMessage)
    },
  })

  const onSubmit = (data: LoginFormData) => {
    // BẮT BUỘC: Gọi mutation thay vì API trực tiếp
    loginMutation.mutate(data)
  }

  return (
    <form onSubmit={handleSubmit(onSubmit)}>
      <div>
        <input
          {...register('email')}
          type="email"
          placeholder="Email"
        />
        {errors.email && <span>{errors.email.message}</span>}
      </div>

      <div>
        <input
          {...register('password')}
          type="password"
          placeholder="Password"
        />
        {errors.password && <span>{errors.password.message}</span>}
      </div>

      <div>
        <input
          {...register('rememberMe')}
          type="checkbox"
        />
        <label>Remember me</label>
      </div>

      <button 
        type="submit" 
        disabled={isSubmitting || loginMutation.isPending}
      >
        {isSubmitting || loginMutation.isPending ? 'Đang xử lý...' : 'Đăng nhập'}
      </button>
    </form>
  )
}
```

**Rules cho React Hook Form**:
- ✅ **BẮT BUỘC**: Sử dụng `useForm` hook cho tất cả forms
- ✅ **BẮT BUỘC**: Sử dụng `register` để register inputs
- ✅ **BẮT BUỘC**: Sử dụng `handleSubmit` cho form submission
- ✅ **BẮT BUỘC**: Sử dụng Zod resolver cho validation
- ✅ **BẮT BUỘC**: Kết hợp với TanStack Query mutations
- ✅ **BẮT BUỘC**: Handle errors từ `formState.errors`
- ❌ **KHÔNG BAO GIỜ**: Dùng controlled inputs với `useState` cho form fields
- ❌ **KHÔNG BAO GIỜ**: Validate form thủ công (dùng Zod schema)

### Best Practices
- **Error Handling**: **BẮT BUỘC** handle errors trong `onError` callback của mutations
- **Loading States**: Sử dụng `isPending`, `isLoading` từ hooks
- **Form Validation**: **BẮT BUỘC** sử dụng React Hook Form với Zod resolver
- **Optimistic Updates**: Có thể dùng `onMutate` cho optimistic updates
- **Cache Invalidation**: **BẮT BUỘC** invalidate queries sau mutations để sync data
- **Query Keys**: Sử dụng consistent query keys (arrays với proper structure)
- **Type Safety**: Type tất cả API responses và requests
- **API Calls**: **BẮT BUỘC** sử dụng TanStack Query, không gọi API trực tiếp

## ♿ Accessibility

### Required Practices
- **Alt Text**: Luôn có `alt` cho images
- **ARIA Labels**: Sử dụng khi cần thiết
- **Keyboard Navigation**: Đảm bảo tất cả interactive elements có thể navigate bằng keyboard
- **Focus Indicators**: Visible focus states
- **Semantic HTML**: Sử dụng đúng HTML elements (`<button>`, `<nav>`, `<main>`, etc.)

### Component Reusability Priority

**QUAN TRỌNG**: Luôn ưu tiên sử dụng components có sẵn trước khi tạo component mới.

#### Priority Order:
1. **shadcn/ui Components** (`src/components/ui/`)
   - Kiểm tra xem component đã có trong `src/components/ui/` chưa
   - Nếu chưa có, cài đặt từ shadcn/ui registry: `npx shadcn@latest add [component-name]`
   - shadcn/ui components đã có sẵn accessibility, keyboard navigation, ARIA labels
   - **Available Components**: button, input, navigation-menu, và nhiều components khác từ shadcn/ui

2. **Custom Components trong Project** (`src/components/`)
   - Kiểm tra xem đã có custom component tương tự chưa
   - Xem trong `src/components/wds/`, `src/components/shop/` hoặc các folder khác
   - Có thể extend/modify component có sẵn thay vì tạo mới

3. **Tạo Component Mới** (chỉ khi không có sẵn)
   - Chỉ tạo component mới khi:
     - Không có trong shadcn/ui registry
     - Không có custom component tương tự trong project
     - Component cần thiết cho business logic đặc thù

#### Examples:

```tsx
// ✅ Good: Sử dụng Button từ shadcn/ui
import { Button } from '@/components/ui/button'

export function ProductActions() {
  return (
    <Button onClick={handleAddToCart}>
      Thêm vào giỏ
    </Button>
  )
}

// ❌ Bad: Tạo button mới thay vì dùng component có sẵn
export function ProductActions() {
  return (
    <button className="px-4 py-2 bg-blue-500" onClick={handleAddToCart}>
      Thêm vào giỏ
    </button>
  )
}
```

```tsx
// ✅ Good: Sử dụng Input từ shadcn/ui
import { Input } from '@/components/ui/input'

export function SearchForm() {
  return <Input type="search" placeholder="Tìm kiếm..." />
}

// ❌ Bad: Tạo input mới
export function SearchForm() {
  return (
    <input 
      type="search" 
      className="px-4 py-2 border rounded"
      placeholder="Tìm kiếm..."
    />
  )
}
```

#### shadcn/ui Component Installation:
```bash
# Cài đặt component từ shadcn/ui
npx shadcn@latest add button
npx shadcn@latest add input
npx shadcn@latest add dialog
npx shadcn@latest add dropdown-menu
# ... và nhiều components khác
```

#### Available shadcn/ui Components:
- **Form Components**: button, input, textarea, select, checkbox, radio-group, switch, label
- **Layout Components**: card, separator, sheet, dialog, drawer, popover, tooltip
- **Navigation**: navigation-menu, breadcrumb, tabs, pagination
- **Data Display**: table, badge, avatar, progress, skeleton
- **Feedback**: alert, toast, alert-dialog
- **Overlay**: dropdown-menu, context-menu, hover-card
- **Và nhiều components khác**: Xem tại https://ui.shadcn.com/docs/components

#### Customization:
- shadcn/ui components có thể customize dễ dàng với Tailwind CSS
- Có thể extend props, thêm variants với `class-variance-authority`
- Giữ nguyên accessibility features khi customize

## 🧪 Testing (Future)

### Testing Strategy
- **Unit Tests**: Jest + React Testing Library
- **E2E Tests**: Playwright (nếu cần)
- **Component Tests**: Test UI components với user interactions

## 📝 Code Quality

### Linting & Formatting
- **ESLint**: Next.js ESLint config với custom rules
- **Prettier**: Auto-format với Tailwind plugin
- **Pre-commit**: Husky + lint-staged

### Best Practices
1. **Component Size**: Giữ components nhỏ, single responsibility
2. **Reusability**: Extract reusable logic vào custom hooks
3. **Performance**: 
   - Lazy load components với `dynamic()` khi cần
   - Optimize images
   - Minimize client-side JavaScript
4. **SEO**: 
   - Metadata cho mỗi page
   - Structured data (JSON-LD)
   - Semantic HTML

## 🚫 Anti-Patterns

### ❌ Don't Do This
- ❌ Dùng `'use client'` không cần thiết
- ❌ Fetch data trong `useEffect` khi có thể dùng Server Components
- ❌ **Gọi API trực tiếp từ component** (phải dùng TanStack Query)
- ❌ **Dùng useState cho form fields** (phải dùng React Hook Form)
- ❌ **Mutation không có onSuccess và onError handlers**
- ❌ Hardcode data thay vì fetch từ API
- ❌ Ignore TypeScript errors
- ❌ Skip alt text cho images
- ❌ Use inline styles thay vì Tailwind classes
- ❌ Create large components (>200 lines)

### ✅ Do This Instead
- ✅ Ưu tiên Server Components
- ✅ Fetch data trong Server Components hoặc dùng `useQuery`
- ✅ **Luôn sử dụng TanStack Query hooks cho API calls**
- ✅ **Luôn sử dụng React Hook Form cho forms**
- ✅ **Luôn có onSuccess và onError trong mutations**
- ✅ Integrate với API endpoints qua TanStack Query
- ✅ Fix TypeScript errors
- ✅ Always provide alt text
- ✅ Use Tailwind utility classes
- ✅ Split large components into smaller ones
